{
  "name": "xml-test",
  "version": "1.0.0",
  "description": "test for litres",
  "main": "parser.js",
  "scripts": {
    "start": "yarn http-server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "anthony",
  "license": "ISC",
  "dependencies": {
    "http-server": "^0.11.1"
  }
}
